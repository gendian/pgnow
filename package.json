{
  "name": "pgnow",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node src/index.js && cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently --kill-others \"node src/index.js\" \"cd client && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "heroku": "^8.4.3",
    "jsdom": "21.1.1",
    "moment": "latest",
    "node-cron": "^3.0.2",
    "node-schedule": "^2.1.1"
  },
  "engines": {
    "node": "16.19.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1"
  }
}
